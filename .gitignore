# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/
env/

# Local tooling
.vscode/
.idea/
.DS_Store
Thumbs.db

# Model checkpoints (regeneratable)
working/model.pt
working/*.pt
working/run.log

# Large raw archive (public/ already contains the extracted CSVs)
public.zip

# Keep logs small: ignore any > baseline training run logs if regenerated
train_run_*.log

# Dataset mirror used only inside Colab (public/ is the source of truth)
dataset/

# Editor swap / temp
*.swp
*~
